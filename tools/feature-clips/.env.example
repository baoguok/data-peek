# Reference only — upload.mjs never reads this file. Populate the real values
# via `op run -- node upload.mjs` (1Password) or `export`ed shell vars. See
# README.md's "Uploading to R2" section. Never fill in real values here.
R2_BUCKET=your-bucket-name
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id
