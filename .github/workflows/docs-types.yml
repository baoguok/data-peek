name: docs types check

on:
  pull_request:
    paths:
      - "apps/docs/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - ".github/workflows/docs-types.yml"
  push:
    branches:
      - main

# A newer commit on the same ref makes the in-flight types check obsolete.
concurrency:
  group: docs-types-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  types-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run docs types:check
        run: pnpm --filter docs types:check
