appId: dev.datapeek.app
productName: Data Peek
executableName: data-peek
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!**/.turbo/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
win:
  executableName: data-peek
nsis:
  artifactName: data-peek-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    CFBundleDisplayName: Data Peek
  hardenedRuntime: true
  gatekeeperAssess: false
  notarize: true
  category: public.app-category.developer-tools
  # arm64 only: the CI runner is Apple Silicon and several native deps (ssh2's
  # sshcrypto, etc.) don't cross-build a correct x64 binary, so an x64 DMG would
  # ship arm64 .node files. Apple Silicon is the only supported mac target.
  target:
    - target: dmg
      arch:
        - arm64
    - target: zip
      arch:
        - arm64
dmg:
  artifactName: data-peek-${version}-${arch}.${ext}
linux:
  target:
    - AppImage
    - deb
    - tar.gz
  maintainer: Rohith Gilla
  category: Development
appImage:
  artifactName: data-peek-${version}-${arch}.${ext}
deb:
  artifactName: data-peek-${version}-${arch}.${ext}
publish:
  provider: github
  owner: Rohithgilla12
  repo: data-peek
